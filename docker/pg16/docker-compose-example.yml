services:
  pgsql:
    image: postgres:16
    restart: no
    environment:
      POSTGRES_USER: your_user
      POSTGRES_PASSWORD: your_password
      POSTGRES_DB: postgres
    ports:
      - 54316:5432
    volumes:
      - .\fs\data:/var/lib/postgresql/data
      - .\fs\scripts:/home/scripts
