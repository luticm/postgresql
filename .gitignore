# Local Docker Compose overrides may contain credentials and machine-specific settings.
**/docker-compose.yml
**/docker-compose.yaml

# PostgreSQL data directories are generated by Docker and can contain thousands of files.
**/fs/data/